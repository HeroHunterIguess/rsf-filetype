# A custom image filetype: .rsf
.rsf is based on a file standard that stores image data in a .rsf file and will display it using pygame. This also contains an encoding algorithm which will later be used to compress file size.

Yes, right now this code is bad!!!!
I know its bad!!!!
I will fix it later!!!!!!!
